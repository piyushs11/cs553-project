akka {
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  loglevel = "INFO"
  logging-filter = "akka.event.slf4j.Slf4jLoggingFilter"
}

# Cinnamon instrumentation config — active only when Cinnamon plugin is enabled.
# Instruments all user actors and collects JVM metrics.
# cinnamon {
#   akka.actors {
#     default-by-class {
#       includes = "/user/*"
#       report-by = class
#     }
#   }
#   chmetrics {
#     reporters += "console-reporter"
#   }
# }